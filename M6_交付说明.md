# M6 系统管理模块 — 交付说明

> 阶段门禁：M6 已完成并通过验证；等待您回复「已确认，执行下一步」后进入 M7（联调与上线）。

## 一、交付内容

### 后端（FastAPI）
| 文件 | 说明 |
|------|------|
| `api/app/schemas/system.py` | 用户 / 角色 / 权限 / 日志 / 部门 的 Pydantic 模型 |
| `api/app/routers/admin/system.py` | 系统管理接口（用户 / 角色权限 / 操作日志 / 部门） |
| `api/app/main.py` | 挂载 `admin_system` 路由（前缀 `/api/admin`） |

**接口清单（服务端 `require_perm` 强制校验，NFR-07）**
- `GET/POST /api/admin/users` — 用户列表 / 新增（system:user）
- `PUT/DELETE /api/admin/users/{id}` — 编辑 / 停用（软删除 is_activate=0，system:user）
- `PUT /api/admin/users/{id}/reset-password` — 重置密码（生成随机强密码并回显，system:user）
- `GET /api/admin/departments` — 部门下拉（system:user）
- `GET/PUT /api/admin/roles` — 角色清单(含权限点全量) / 配置权限点（system:role）
- `GET /api/admin/logs` — 操作日志分页查询（只读，system:log）

### 前端（后台 AntD）
| 文件 | 说明 |
|------|------|
| `backend/src/api/index.ts` | 新增 `systemApi`（users/roles/logs/departments） |
| `backend/src/pages/system/UserList.tsx` | 用户管理：列表 + 抽屉新增/编辑 + 停用启用 + 重置密码弹窗 |
| `backend/src/pages/system/RoleList.tsx` | 角色权限：勾选式配置，权限点按菜单分组（全选/单条） |
| `backend/src/pages/system/LogList.tsx` | 操作日志：只读分页 + 模块/动作/关键字/日期筛选 |
| `backend/src/App.tsx` | 接入 3 条路由（/system/users · /system/roles · /system/logs） |
| `backend/src/layouts/AdminLayout.tsx` | 「系统管理」手风琴菜单，子项按权限显隐 |

## 二、关键约束落地
- **RBAC 服务端强制**：`require_perm` 校验权限码；前端仅隐藏入口。
- **内置超管保护**：`super_admin` 角色权限不可改（返回 40300）。
- **自我保护**：禁止停用/降权当前登录账号（返回 40300）。
- **首次登录改密**：新增用户 `must_change_pwd=True`；重置密码后同样强制改密（BR-06）。
- **M6-4 权限点全量清单**：固化于 `api/scripts/seed.py` 的 `PERMISSIONS`（38 项，按后台菜单/功能拆分），种子已注入；`GET /roles` 同时返回全量清单供勾选回显。
- **操作日志**：关键操作（登录/增删改/停用/重置密码/角色权限更新）自动写入 `sys_operation_log`，只读查询。

## 三、验证结果
| 验证项 | 结果 |
|--------|------|
| 后端 FastAPI TestClient 冒烟 | 19 项全通过（含超管保护 403、自身保护 403、角色权限更新生效、停用后列表不可见） |
| 运行后端 HTTP 实测 | `GET /users`、`/roles`、`/logs`、`/departments` 均 200 |
| 前端 `npm run build`（tsc -b + vite build） | 0 错误（仅 chunk 体积预警，非阻断） |

## 四、Git
- 提交 `518cc16` 已推送 `origin/main`：`M6: 系统管理模块（用户/角色权限/操作日志/部门 + 前端三页面 + 端到端验证通过）`
- 文档：`docs/STK本然家居_项目开发实施方案.md` 追加 M6 工作日志（§11）。

## 五、下一步（M7 联调与上线）
前后端全量联调 / UI 走查 / 性能安全检查 / 数据初始化 / Docker Compose 一键部署。
