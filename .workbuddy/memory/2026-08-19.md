# 2026-08-19 工作日志

## STK 本然家居官网 — 开发启动

- **方案文档**：撰写《STK本然家居_项目开发实施方案.md》V1.0，基于 4 份基线文档（PRD V1.9 / UI-UX V1.0 / 数据库设计 V1.3 / 开发技术文档 V1.2），按 M1~M7 分阶段门禁推进；存放于 `docs/`。
- **虚拟环境**：按用户截图规范创建 `api/.venv`（Python 3.13.14），已装 fastapi 0.141.1 / uvicorn 0.52.4 / SQLAlchemy 2.0.52 / PyJWT 2.13.0 / bcrypt 5.0.0 / python-multipart 0.0.32 / aiofiles 25.1.0；`api/requirements.txt` 已生成（pip freeze 锁版本）。
  - ⚠️ 教训：沙箱环境 `pip install --upgrade pip` 会触发 Windows 回收站机制失败（safe-delete 被拒），导致 pip 被卸载；用 `python -m ensurepip --upgrade` 恢复后跳过 pip 升级直接装包。
- **Git 初始化**：远程 `git@github.com:my123-hub/dev_master.git`（公开仓库，空）已关联为 origin，本地首次提交 `bc3c4db` 并推送 main 成功（含 .gitignore、docs/、api/requirements.txt、两个原型 HTML）。
  - 仓库级身份：user.name=my123-hub，user.email=my123-hub@users.noreply.github.com。
  - GitHub SSH 22 端口偶发超时，重试即可（认证本身正常）。

## 待办
- 等待用户确认 Q1~Q7 待确认事项（门店/联系信息真实值、Slogan、演示数据、设计收敛、初始密码、运行方式、首页模块顺序）。
- 确认后推进 M1：后端骨架 + 21 张表 SQLAlchemy 模型 + Alembic 初始迁移 + 种子脚本 + JWT 认证。
